{
  "name": "external-agent-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local MCP runtime for delegating asynchronous jobs to external CLI coding agents.",
  "bin": {
    "external-agent-mcp": "./src/server.mjs"
  },
  "scripts": {
    "start": "node ./src/server.mjs",
    "test": "node ./test/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
